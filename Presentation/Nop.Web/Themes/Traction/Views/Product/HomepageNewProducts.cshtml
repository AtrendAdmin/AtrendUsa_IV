@model IList<ProductOverviewModel>

@using Nop.Web.Models.Catalog;

@if (Model.Count > 0)
{
    <div class="product-grid home-page-new-products">
        <div class="title">
            <strong>@T("HomePage.NewProducts")</strong>
        </div>
        <div class="item-grid">
            @foreach (var item in Model)
            {
                <div class="item-box">
                    @Html.Partial("_ProductBox_NoPrice", item)
                </div>
            }
        </div>
    </div>
}